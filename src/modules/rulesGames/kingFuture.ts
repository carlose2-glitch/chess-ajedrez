import type { GameManipulator, RulesPieces } from '../interfaces/pieces.interface';

export const kingFuture = (c: number, r: number, orderGame: GameManipulator[], name: string) => {
  const array: RulesPieces[] = [];

  return array;
};
